source $HOME/.zprezto/runcoms/zshrc

eval $(/opt/homebrew/bin/brew shellenv)

export ZPLUG_HOME=/opt/homebrew/opt/zplug
source $ZPLUG_HOME/init.zsh

# Plugins
zplug "plugins/git",   from:oh-my-zsh
zplug "plugins/osx",   from:oh-my-zsh
zplug "zsh-users/zsh-autosuggestions"
zplug "spaceship-prompt/spaceship-prompt", use:spaceship.zsh, from:github, as:theme
zplug 'ryutok/rust-zsh-completions'
zplug "jeffreytse/zsh-vi-mode"

# Install any declared-but-missing plugins (keeps a fresh machine reproducible)
if ! zplug check; then
  zplug install
fi

zplug load

# Spaceship prompt config (see https://spaceship-prompt.sh/config/intro/)
# Override defaults by exporting SPACESHIP_* vars here, e.g.:
# SPACESHIP_PROMPT_ADD_NEWLINE=true
# SPACESHIP_CHAR_SYMBOL="➜ "

# brew jenv
export PATH="$HOME/.jenv/bin:$PATH"
eval "$(jenv init -)"

# brew postgresql@17
export PATH="/opt/homebrew/opt/postgresql@17/bin:$PATH"

. "$HOME/.cargo/env"

# bun completions
[ -s "/Users/ohe/.bun/_bun" ] && source "/Users/ohe/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/ohe/.lmstudio/bin"
# End of LM Studio CLI section

# Scaleway CLI autocomplete initialization.
eval "$(scw autocomplete script shell=zsh)"
